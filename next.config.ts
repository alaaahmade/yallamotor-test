import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: ["ymimg1.b8cdn.com", "yallamotor-production-assets.s3.ap-south-1.amazonaws.com"],
  },
};

export default nextConfig;
